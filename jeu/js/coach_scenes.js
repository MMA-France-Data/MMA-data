/**
 * coach_scenes.js — LE CONTENU DES CONVERSATIONS AVEC LES COACHS.
 * GÉNÉRÉ. Ne pas éditer à la main pour de petites retouches : c'est une
 * DONNÉE, pas du code. La mécanique qui joue tout ça est dans
 * coach_dialogue.js ; le banc 39 vérifie CE fichier comme une donnée.
 */
const SCENES = { bureau: [], bord_du_tapis: [], debrief: [], accrochage: [], porte: [] };
module.exports = { SCENES };
